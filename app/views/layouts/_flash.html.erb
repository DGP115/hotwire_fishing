<div class="flashes">
  <% flash.each do |type, message| %>
    <%= content_tag(:div, message,
                          class: "flash #{type}",
                          data: { controller: "flash-message-display" }) %>
  <% end %>
</div>
